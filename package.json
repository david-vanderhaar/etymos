{
  "name": "etymos",
  "private": true,
  "version": "0.0.4",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "NODE_OPTIONS='--max-old-space-size=8192' vite build",
    "publish": "pnpm run build && pnpm run deploy",
    "preview": "pnpm run build && vite preview",
    "deploy": "touch build/.nojekyll && gh-pages -d build -t true"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "dependencies": {
    "@fontsource-variable/cormorant": "^5.0.9",
    "@sveltejs/adapter-static": "^2.0.2",
    "@sveltejs/kit": "^1.18.0",
    "gh-pages": "^6.1.1",
    "mitt": "^3.0.1",
    "quill": "2.0.2",
    "quill-autoformat": "^0.1.2",
    "svelte": "^3.59.1",
    "svelte-icons": "^2.1.0",
    "tippy.js": "^6.3.7",
    "tree-model": "^1.0.7"
  }
}
